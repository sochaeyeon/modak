<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.modak.badword.mapper.BadWordMapper">

    <select id="selectAllWords" resultType="string">
        SELECT WORD FROM BAD_WORD ORDER BY WORD_ID ASC
    </select>

    <select id="selectAllWordsWithId" resultType="map">
        SELECT WORD_ID AS wordId, WORD AS word,
               DATE_FORMAT(CREATED_AT, '%Y-%m-%d %H:%i') AS createdAt
        FROM BAD_WORD
        ORDER BY WORD_ID DESC
    </select>

    <insert id="insertWord" parameterType="string">
        INSERT INTO BAD_WORD (WORD, CREATED_AT) VALUES (#{word}, NOW())
    </insert>

    <delete id="deleteWord" parameterType="long">
        DELETE FROM BAD_WORD WHERE WORD_ID = #{wordId}
    </delete>

</mapper>