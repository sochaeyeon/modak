<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.modak.csCenter.mapper.CsCenterMapper">

	<select id="getFaqList" parameterType="hashmap" resultType="hashmap">
    SELECT
        faq_id AS faqId, category, sub_category AS subCategory,
        question, answer,
        DATE_FORMAT(created_at, '%Y-%m-%d') AS createdAt
    FROM faq
    WHERE 1=1
    <if test="categoryList != null and categoryList.size() > 0">
        AND (category IN
        <foreach collection="categoryList" item="cat" open="(" separator="," close=")">
            #{cat}
        </foreach>
        OR sub_category IN
        <foreach collection="categoryList" item="cat" open="(" separator="," close=")">
            #{cat}
        </foreach>
        )
    </if>
    <if test="excludeList != null and excludeList.size() > 0">
        AND category NOT IN
        <foreach collection="excludeList" item="cat" open="(" separator="," close=")">
            #{cat}
        </foreach>
        AND (sub_category IS NULL OR sub_category NOT IN
        <foreach collection="excludeList" item="cat" open="(" separator="," close=")">
            #{cat}
        </foreach>
        )
    </if>
    <if test="searchKeyword != null and searchKeyword != ''">
        AND (question LIKE CONCAT('%', #{searchKeyword}, '%')
        OR answer LIKE CONCAT('%', #{searchKeyword}, '%'))
    </if>
    ORDER BY created_at DESC
    LIMIT 4
</select>

	<select id="getNotificationList" parameterType="hashmap"
		resultType="hashmap">
		SELECT notification_id AS notificationId, title,
		type, is_pinned AS isPinned, view_count AS viewCount,
		DATE_FORMAT(created_at, '%Y-%m-%d') AS createdAt FROM notification
		ORDER BY is_pinned DESC, created_at DESC LIMIT
		#{limit}
	</select>

</mapper>