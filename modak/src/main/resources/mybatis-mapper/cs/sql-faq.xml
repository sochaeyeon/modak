<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.modak.csCenter.mapper.FaqMapper">

	<select id="selectFaqList" parameterType="hashmap"
		resultType="com.example.modak.csCenter.model.Faq"> SELECT FAQ_ID AS
		faqId, CATEGORY AS category, QUESTION AS question, ANSWER AS answer,
		CREATED_AT AS createdAt FROM faq <where>
			<if test="category != null and category != ''">
				AND CATEGORY = #{category}
			</if>
			<if test="searchKeyword != null and searchKeyword != ''">
				AND (QUESTION LIKE CONCAT('%', #{searchKeyword}, '%')
				OR ANSWER LIKE CONCAT('%', #{searchKeyword}, '%'))
			</if>
		</where>
		ORDER BY FAQ_ID DESC </select>

</mapper>