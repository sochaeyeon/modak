<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.modak.event.mapper.EventMapper">
<!--  이벤트 목록 조회 (탭 필터 + 페이징)  -->
<select id="selectEventList" parameterType="hashmap" resultType="com.example.modak.event.model.Event">
SELECT EVENT_ID AS eventId, TITLE AS title, CONTENT AS content, START_DATE AS startDate, END_DATE AS endDate FROM EVENT
<where>
<!--  tabType: 'ongoing'=진행중 / 'ended'=종료 / 'winner'=당첨자 발표  -->
<if test="tabType != null and tabType == 'ongoing'"> <![CDATA[ AND END_DATE >= NOW() ]]></if>
<if test="tabType != null and tabType == 'ended'"> <![CDATA[ AND END_DATE < NOW() ]]></if>
</where>
ORDER BY EVENT_ID DESC 
LIMIT #{pageSize, jdbcType=INTEGER} 
OFFSET #{offset, jdbcType=INTEGER}
</select>
<!--  전체 건수 (페이징용)  -->
<select id="selectEventCount" parameterType="hashmap" resultType="int">
SELECT COUNT(*) FROM EVENT
<where>
<if test="tabType != null and tabType == 'ongoing'"><![CDATA[ AND END_DATE >= NOW() ]]></if>
<if test="tabType != null and tabType == 'ended'"><![CDATA[ AND END_DATE < NOW() ]]></if>
</where>
</select>
<!--  단건 상세 조회  -->
<select id="selectEvent" parameterType="hashmap" resultType="com.example.modak.event.model.Event"> 
SELECT EVENT_ID AS eventId, TITLE AS title, CONTENT AS content, START_DATE AS startDate, 
END_DATE AS endDate FROM event WHERE EVENT_ID = #{eventId} 
</select>
</mapper>