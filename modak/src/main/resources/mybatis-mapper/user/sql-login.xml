<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.modak.user.mapper.LoginMapper">

	<!-- 로그인 -->
	<select id="selectUser" parameterType="hashmap"
		resultType="com.example.modak.user.model.User">
		SELECT *
		FROM USER
		WHERE USER_ID = #{userId} AND USER_STATUS = 'ACTIVE'
	</select>

	<!-- 소셜 계정 조회 -->
	<select id="selectUserBySocial" parameterType="hashmap"
		resultType="com.example.modak.user.model.User">
		SELECT *
		FROM `USER`
		WHERE SOCIAL_TYPE = #{socialType}
		AND SOCIAL_ID = #{socialId}
	</select>
	
	<!-- 소셜 로그인 -->

	<!-- USER_ID 중복 체크 -->
	<select id="countUserId" parameterType="hashmap" resultType="int">
		SELECT COUNT(*)
		FROM `USER`
		WHERE USER_ID = #{userId}
	</select>

	<!-- 소셜 회원 등록 -->
	<insert id="insertSocialUser" parameterType="hashmap">
		INSERT INTO `USER` (
		USER_ID,
		USER_PWD,
		USER_NAME,
		EMAIL,
		NICKNAME,
		USER_STATUS,
		CREATED_AT,
		SOCIAL_TYPE,
		SOCIAL_ID
		) VALUES (
		#{userId},
		'SOCIAL',
		#{userName},
		#{email},
		#{nickName},
		'ACTIVE',
		NOW(),
		#{socialType},
		#{socialId}
		)
	</insert>
	
</mapper>